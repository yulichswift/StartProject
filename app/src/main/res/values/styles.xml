<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryDark">@color/purple</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:editTextColor">@android:color/black</item>
        <item name="android:textAllCaps">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TransparentBarTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="TranslucentStatusTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="TranslucentTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.App.Starting.TransparentBar" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_smile</item>
        <item name="postSplashScreenTheme">@style/TransparentBarTheme</item>
    </style>

    <style name="Theme.App.Starting.AppTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_smile</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <style name="btn_size_14">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="FloatingHintTextStyle" parent="TextAppearance.Design.Hint">
        <item name="android:textColor">#08f</item>
        <item name="android:textStyle">italic</item>
    </style>

</resources>
